Header
  + name := N_arg
Inherit
  + parent_node:Flat Node
  
Public
  
  + type:Type
  - set_type t:Type :Self <- ( type := t; self)
  
  + is_rec:Boolean
  - set_rec r:Boolean :Self <- ( is_rec := r; self)
  
  - new t:Type rec r:Boolean :Self <- clone.set_type t.set_rec r
  
  - append_in b:String_buffer <-
  (
    is_rec.if { b | '['; }
    b | '('
    (type = NULL).if {
      b += "NULL"
    } else {
      type.append_in b
    }
    b | ')'
    is_rec.if { b | ']'; }
  )
  
