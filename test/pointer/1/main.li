Header
  + name := Main
Public
  - main <-
  ( + p:Pointer
    Test.check (p = NULL); // should be... in the next release.
    p := `"toto"`:Pointer
    Test.check (p != NULL); // should be... in the next release.
  )
  
