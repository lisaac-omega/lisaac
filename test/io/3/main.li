Header
  + name := Main
Public
  
  - eb:Flat String_buffer
  
  - main <-
  (
    eb.copy "toto"
    String.tmp { tmp:String_buffer
      eb.foreach {c:Char
        tmp.add_last c
      }
      Test.check (tmp.to_string_alias = "toto")
    }
  )
