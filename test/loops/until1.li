Header
  + name := Until1
/**/
  
    
Public
  
  - main <-
  ( + i:Int
    Test.check (i = 0)
    until {i = 0} do {
      i := i + 1
    }
    Test.check (i = 0)
    until {i >= 1} do {
      i := i + 1
    }
    Test.check (i = 1)
  )
