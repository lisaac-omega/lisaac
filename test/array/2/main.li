Header
  + name := Main
Insert
  - parent_print:Print := Print
    
Public
  - main <-
  ( + sb: Flat String_buffer
    //TEST.check_null (sb.storage)
    Test.check (sb.capacity = 0)
    Test.check (sb.is_empty)
  )
