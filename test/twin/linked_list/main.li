Header
  + name := Main
Public

  - main <-
  ( + l1,l2:Linked_list Char
    l1 := Linked_list Char .new
    l1.add_last '2'
    l2 := l1.twin
    Test.check (l1.upper = l2.upper)
    Test.check (l1.first = l2.last)
    //UTILS.exit 0
    //("bug " + "init_string_alias").to_string_alias
  )

