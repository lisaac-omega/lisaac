Header
  + name := Ifthendot
/*

  */

Public
  
  - main <-
  (
    (if True then {
        Test.check True
    }) elseif {False} then {
      Test.check True      
    }
    Test.check (Test.counter = 1)
  );
  