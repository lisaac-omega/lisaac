Header
  + name := APPEND_UTF8;
  
Public
  
  + str:STRING_BUFFER;
  
  - main <-
  (
    str := STRING_BUFFER.create 4
    TEST.check (str.count = 0)
    str.append_utf8 "é€"
    TEST.check (str.count = 2)
    TEST.check (str == "é")
  )
  
  
