Header
  + name := Pack_range
Mapping
  + font_size:Real32;
  + first_unicode_codepoint_in_range:Int32; 
  + array_of_unicode_codepoints:Array Int32;
  + num_chars:Int32;
  + chardata_for_range:Array Packed_char; 
  + h_oversample:Uint8;
  + v_oversample:Uint8; 
  
Public  
/*  
  - pack_font_range (spc:ARRAY PACK_CONTEXT,fontdata:ARRAY Uint8,
    font_index:Int,size:REAL_32,first_unicode:Int, 
  n_char:Int,chardata:PACKED_CHAR) :Int <-
  ( 
    // BSBS: Il faut faire ça dans un clone avant !
    first_unicode_codepoint_in_range := first_unicode
    array_of_unicode_codepoints := NULL
    num_chars                   := n_char
    chardata_for_range          := chardata
    font_size                   := size
    pack_font_ranges (spc, fontdata, font_index, &range, 1)
  )
*/
