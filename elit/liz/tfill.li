Header
  + name := Tfill
Inherit
  - parent_texp:Texp := Texp
  
Public
  
  - run <-
  (
    fill_color (Look.cols.at (Look.slot))
    fill
    fill_color (Tok_drw.last_color)
  )
  
  - append_in b:String_buffer <- ( b += "Fill"; )
